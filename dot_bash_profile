# =============================================================================
# Bash ログインシェル設定 (~/.bash_profile)
# =============================================================================

# ~/.bashrc があれば読み込む
if [ -f "$HOME/.bashrc" ]; then
  . "$HOME/.bashrc"
fi
